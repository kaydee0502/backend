language: ruby
rvm:
- 2.7.2
- 2.0.0
- 1.9.3
- jruby-19mode

script:
 - cd devsnest
 - bundle install
 - bundle exec rake db:create
 - bundle exec rake db:migrate
 - bundle exec rspec
branches:
  only:
    - user-profile-tests
